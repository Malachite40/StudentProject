<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dylan.cronkhite_madc196">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TermsView" />
        <activity android:name=".MentorsView" />
        <activity android:name=".TermMainView" />
        <activity android:name=".EditTermView" />
        <activity android:name=".EditCourse" />
        <activity android:name=".CourseView" />
        <activity android:name=".CourseDetails" />
        <activity android:name=".EditMentor" />
        <activity android:name=".AssesmentView" />
        <activity android:name=".EditAssesmentView"></activity>
    </application>

</manifest>